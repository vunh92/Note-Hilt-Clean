<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vunh.note_hilt_clean">

    <application
        android:name=".NoteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteHiltClean">
        <activity
            android:name=".presentation.update_note.UpdateNoteActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.add_note.AddNoteActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.notes.ScreenNoteActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>